# claude_notes

Maintained, committed docs for working on this repo (travels with `git clone`).

**Current / maintained:**
- `SETUP.md` — set up a fresh machine and train the baseline (clone → env → data → run).
- `EXPERIMENT_WORKFLOW.md` — how to add, run, and document an experiment.

See also the top-level [`CLAUDE.md`](../CLAUDE.md) (agent entry point),
[`README.md`](../README.md), and [`docs/`](../docs).

**Historical (unmaintained — predate the YAML config + GAConv work):** these describe
the original notebook → package refactor. Treat as background, not current truth; for
current config/run/architecture see CLAUDE.md, SETUP.md, and docs/.
- `IMPLEMENTATION.md` — per-module reference written during the refactor (mentions the
  now-removed `config.py` and FPN@128; superseded).
- `REFACTORING_NOTES.md` — notebook → package change log and rationale.
- `SUMMARY.md` — early research context / multi-week plan.
