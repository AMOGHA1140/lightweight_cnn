# Core (all pipelines)
torch>=2.1
torchvision>=0.16
numpy
Pillow
tqdm

# OBB detector: rotated IoU. Install ONE of these (mmcv preferred -> CUDA op).
# mmcv>=2.0          # preferred: common.rotated_ops uses mmcv.ops.box_iou_rotated
shapely              # CPU fallback for rotated IoU

# FLOPs reporting
thop

# Backbone pretraining: local logging
tensorboard

# Faster R-CNN experiment
opencv-python

# YOLO comparison pipeline
ultralytics
pandas
matplotlib
seaborn
pyyaml
