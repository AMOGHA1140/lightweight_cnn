# Core (all pipelines)
torch>=2.1
torchvision>=0.16
numpy
Pillow
tqdm

# OBB detector: rotated IoU + NMS (required). Install via openmim to match torch/CUDA:
#   pip install -U openmim && mim install mmcv
mmcv>=2.0

# FLOPs reporting
thop

# Backbone pretraining: local logging
tensorboard

# Faster R-CNN experiment
opencv-python

# YOLO comparison pipeline
ultralytics
pandas
matplotlib
seaborn
pyyaml
